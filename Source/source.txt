PLAYERS
[
	players[4];
	masters[2];
]

STATE 
[
	INT a = 1;
	BOOL b = false;
	INT c = 5;
]
([INT var = 9;]
{
	$.a = var % $.c;
	$.b = var < $.a && (var >= 9);
})

MAIN_RULE
[
	r1
	([]{ RETURN true; }) 
	players
	[]{RETURN 1;}
	masters
	[]{#.a = 2; RETURN a;}
]

MOVES
[
	m1<*> [ INT a = 0;] ([]{ RETURN; })
	[]
	{
		RETURN;
	}
	%
	m2<masters> [ BOOL b = false; ] ([]{ RETURN; })
	[]
	{
		a = a > b || w >= 7 && 8 != a; 
	}
]